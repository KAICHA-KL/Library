const mylibrary = [];

function book(title,genre,author,pages){
 this.bookTitle = title;
 this.bookGenre = genre;
 this.bookAuthor = author;
 this.bookPages = pages;
}

function addBookToLibrary() { 

}